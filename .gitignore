# simulation
test/sim_build/
test/__pycache__/
# the suites import tools/sdwrite.py by path, so this appears there too
tools/__pycache__/
test/results*.xml
test/*.fst
test/*.vcd
# tb_bram_cart.v is plain Verilog run straight from the repo ROOT (iverilog +
# vvp, no cocotb), so its dump and binary land here rather than under test/.
*.fst
*.vcd
*.vvp
# fpga
fpga/build/
# build artifacts (game.bin IS committed: test/test_rehearse.py loads it)
sw/*.elf
